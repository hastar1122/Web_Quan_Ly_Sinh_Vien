@model IEnumerable<tblDiem>

@foreach (var item in Model) 
{
<li class="border-left-info mb-1">
    <a style="cursor:pointer;" data-id="@item.MaSV" class="list-group-item list-group-item-action border-0 px-2">
        <div class="d-flex align-items-start">
            <img src="@Url.Content(item.tblUser.Anh)" class="rounded-circle mr-1" alt="Vanessa Tucker" width="40" height="40">
            <div class="flex-grow-1 ml-2">
                <strong>@item.tblUser.Ho @item.tblUser.Ten</strong>
                @{
                    <!--if (@item.tblUser.ConnectionId != null)
                    {
                        <div class="small float-right mt-2"><span class="fas fa-circle user-@item.MaSV chat-online"></span>--> @*Online*@<!--</div>
                    }
                    else
                    {
                        <div class="small float-right mt-2"><span class="fas fa-circle user-@item.MaSV chat-offline"></span>--> @*Offline*@<!--</div>
                    }-->
                }
                <div class="small">
                    @item.tblUser.MaSV
                </div>
            </div>
        </div>
    </a>
</li>
}