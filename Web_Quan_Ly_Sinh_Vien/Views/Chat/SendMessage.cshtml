@model tblTinNhan

<div class="chat-message-right pb-2">
    <div>
        <img src="@Url.Content(Model.tblUser.Anh)" class="rounded-circle mr-1" alt="Chris Wood" width="40" height="40">
        <div class="text-muted small text-nowrap mt-2">@String.Format("{0:t}", Model.ThoiGian)</div>
    </div>
    <div class="flex-shrink-1 bg-primary rounded py-2 px-3 mr-3 text-white">
        <div class="font-weight-bold mb-1 text-right">Bạn</div>
        <span class="text-break">@Model.NoiDung</span>
    </div>
</div>
